/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/**/*.{ts,tsx}'],
    theme: {
        extend: {
            colors: {
                // CUSTOM COLORS HERE
            },
            fontFamily: {
                // CUSTOM FONTS HERE
            }
        },
    },
    plugins: [],
}